import React, { FC } from 'react';
import './style.css';

interface IProps {}

const Tweet: FC<IProps> = ({}) => (
  <div className="tweet--wrapper-main">
    <h1>Tweet</h1>
  </div>
);

export default Tweet;
